streamlit>=1.36.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.22.0
pypdf>=4.2.0
python-docx>=1.1.0
pytest>=8.0.0
# Optional semantic layer can be added later; the baseline works without it.

# Run with:
#   pip install -r requirements.txt
#   streamlit run app/main.py

# The baseline uses deterministic TF-IDF similarity and does not download a transformer model.
# A sentence-transformer semantic layer can be added as a future, separately evaluated upgrade.

# Python 3.10+
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.5.0
